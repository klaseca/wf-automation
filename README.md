# Tools for automation warface

## Compile toolchain

- clang
- make

## Compiling:

1. Run command `make` in utils directory
2. Run command `make` for debug app or `make release` for release app in needed directory tool
